# derived cache — rebuilt by `add sync`, never authoritative, never committed
.add/graph.json

__pycache__/
.pytest_cache/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.serena/
tmp/

# agent worktrees — transient, and each is its own git repo
.claude/worktrees/
